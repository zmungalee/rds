# Infrastructure Notes

- Local orchestration is managed with `docker-compose.yml` at the repo root.
- Postgres bootstrap SQL can be placed in `sql/init.sql`.
- Add production IaC (Terraform/Pulumi/Helm) under this folder later.
